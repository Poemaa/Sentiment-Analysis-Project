@model IEnumerable<SentimentAnalysis_Project.Models.Fakulteti>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FakultetiId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TitulliDiplomimit)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FakultetiId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TitulliDiplomimit)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Dega">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Dega">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Dega">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
